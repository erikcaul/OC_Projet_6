/**
 * Film's management in object
 */

class FilmManager{
    constructor(listFilm){
        this.listFilm = listFilm;
    }
/* 
    sort(){
        this.listFilm.sort((a,b)) => {
            return (a.imdb_score < b.imdb_score)?1:-1;
        }}; */
}